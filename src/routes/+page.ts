// Landing page - mostly static content
// Data prefetching happens in +layout.svelte (background)

// Disable SSR for consistent behavior with rest of app
export const ssr = false;

// Could prerender the landing page in the future
export const prerender = false;
